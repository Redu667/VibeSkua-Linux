<UserControl
    x:Class="Skua.WPF.Views.AppUpdaterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:managerVMs="clr-namespace:Skua.Core.ViewModels.Manager;assembly=Skua.Core"
    d:DataContext="{d:DesignInstance managerVMs:AppUpdaterViewModel}"
    mc:Ignorable="d"
    d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="16">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <materialDesign:PackIcon
                Kind="Update"
                Width="100"
                Height="100"
                HorizontalAlignment="Center"
                Foreground="{DynamicResource PrimaryHueMidBrush}"
                Margin="0,0,0,24" />

            <TextBlock
                Text="Skua Client Updater"
                Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                HorizontalAlignment="Center"
                Margin="0,0,0,8" />

            <TextBlock
                Text="{Binding UpdateStatus}"
                Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                HorizontalAlignment="Center"
                Margin="0,0,0,24" />

            <ProgressBar
                Value="{Binding ProgressValue}"
                Minimum="0" Maximum="100"
                Height="10"
                Margin="0,0,0,24"
                Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button
                    Command="{Binding CheckForUpdateCommand}"
                    Content="Check for Updates"
                    IsEnabled="{Binding IsChecking, Converter={StaticResource InvertBooleanConverter}}"
                    Margin="0,0,16,0"
                    Width="160" />

                <Button
                    Command="{Binding DownloadAndInstallCommand}"
                    Content="Update &amp; Restart"
                    Visibility="{Binding UpdateAvailable, Converter={StaticResource BooleanToVisibilityConverter}}"
                    IsEnabled="{Binding IsChecking, Converter={StaticResource InvertBooleanConverter}}"
                    Style="{StaticResource MaterialDesignRaisedAccentButton}"
                    Width="160" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
