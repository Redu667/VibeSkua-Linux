<UserControl
    x:Class="Skua.WPF.Views.HotKeysView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Skua.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vms="clr-namespace:Skua.Core.ViewModels;assembly=Skua.Core"
    d:DataContext="{d:DesignInstance vms:HotKeysViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" 
                      ItemsSource="{Binding FilteredCustomActions}" 
                      SelectedItem="{Binding SelectedCustomAction, Mode=TwoWay}"
                      Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      IsEditable="True" 
                      StaysOpenOnEdit="True"
                      IsTextSearchEnabled="False"
                      TextSearch.TextPath="Title"
                      PreviewMouseLeftButtonUp="ComboBox_PreviewMouseLeftButtonUp"
                      KeyUp="ComboBox_KeyUp">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" ToolTip="{Binding Description}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Grid.Column="1" Content="Add Custom Hotkey" Command="{Binding AddCustomCommand}" Margin="5,0,0,0" Padding="10,2"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ItemsControl
                Margin="3"
                Grid.IsSharedSizeScope="True"
                ItemsSource="{Binding HotKeys}" />
        </ScrollViewer>
    </Grid>
</UserControl>